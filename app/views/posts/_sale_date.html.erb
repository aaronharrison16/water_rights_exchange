<% if @post.sale_status == 'sale_pending' %>
    <div>
        <%= link_to 'Mark as Sold', sold_post_path(@post), class: 'btn btn-mark-sold mb-1' %>
    </div>
<% else %>
    <%= form_for @post, class: 'form-horizontal' do |f| %>
            <h4 for="date" class="control-label">Sale Date</h4>
        <div class='row'>
            <%= f.date_field :sale_date, class: 'ml-3 form-control form-control-custom col-md-4', id: 'date' %>
            <%= f.submit 'Save', class: "ml-2 btn btn-custom col-md-2" %>
        </div>
    <% end %>
<% end %>