<%= form_for @comment, url: '#' do |f| %>
    <div class='form-group'>
        <%= f.label :content %>
        <%= f.text_area :content, class: 'form-control' %>
    </div>
    <%= f.submit 'Leave Comment', class: 'btn btn-custom' %>
<% end %>